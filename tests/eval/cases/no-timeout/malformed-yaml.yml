# eval: rule=no-timeout expect=silent kind=fp
# why: malformed YAML returns null from parser — no rules fire on unparseable input
# source: cross-cutting case — parser error handling
name: [Malformed YAML
on:
  push:
    branches: [main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo hi
