# eval: rule=queue-dominated expect=silent kind=fp
# why: median queue 1 min vs median run 10 min (10%) is well below 50% threshold
# source: normal queue time pattern — short wait before job starts
name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - run: cargo build
