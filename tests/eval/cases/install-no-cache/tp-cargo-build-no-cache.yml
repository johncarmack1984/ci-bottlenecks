# eval: rule=install-no-cache expect=fire kind=tp count=1
# why: cargo build without any Rust cache mechanism should fire
# source: rule spec — uncached Rust build
name: cargo build without cache
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - run: cargo build
      - run: cargo test
