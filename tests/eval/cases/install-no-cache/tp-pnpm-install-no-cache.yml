# eval: rule=install-no-cache expect=fire kind=tp count=1 pedantic=true
# why: pnpm install without any cache mechanism should fire (unmeasured detection: pedantic-only info trace)
# source: rule spec — uncached pnpm install
name: pnpm install without cache
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - run: npm install -g pnpm
      - run: pnpm install
      - run: pnpm test
