# eval: rule=setup-dominated expect=silent kind=fp
# why: empty runs array triggers early return before any analysis
# source: edge case — no timing data available
name: CI
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - run: npm test
