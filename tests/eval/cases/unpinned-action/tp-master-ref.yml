# eval: rule=unpinned-action expect=fire kind=tp
# why: action pinned to @master is a mutable ref — can change without notice
# source: rule spec — @master is explicitly flagged
name: Action at master
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: some-org/some-action@master
      - run: echo done
