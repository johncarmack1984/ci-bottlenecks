# eval: rule=unpinned-action expect=silent kind=fp
# why: docker:// actions are skipped — they use container image tags, not git refs
# source: rule spec — docker:// prefix excluded
name: Docker action
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: docker://alpine:3.18
      - run: echo done
