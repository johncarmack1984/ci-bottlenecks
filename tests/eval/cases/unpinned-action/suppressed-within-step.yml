# eval: rule=unpinned-action expect=silent kind=fp
# why: suppression comment within step block silences unpinned-action for that step only
# source: cross-cutting case — suppression system step-level syntax
name: Suppressed within step
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: some-org/some-action@main # ci-bottlenecks: ignore[unpinned-action]
      - run: echo done
