# eval: rule=double-run-measured expect=fire kind=tp count=1
# why: 2 runs with null job timestamps fall back to createdAt->updatedAt for duration
# source: fallback duration calculation when job timing is unavailable
name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - run: npm test
