# eval: rule=double-run-measured expect=fire kind=tp count=1
# why: 2 runs on same SHA within 30 seconds should fire
# source: basic double-trigger pattern
name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - run: npm test
