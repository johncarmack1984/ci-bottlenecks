# eval: rule=double-run-measured expect=silent kind=fp
# why: 2 runs on same SHA 10 minutes apart exceeds the 5-minute window
# source: legitimate re-run after investigation
name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - run: npm test
