# eval: rule=no-concurrency expect=fire kind=tp
# why: pull_request trigger with no concurrency block at all
# source: PR CI workflow missing concurrency configuration
name: PR Check
on:
  pull_request:
jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - run: npm test
